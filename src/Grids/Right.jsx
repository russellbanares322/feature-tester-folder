import { Box } from "@mui/material";
import React from "react";

const Right = () => {
  return (
    <Box sx={{ background: "cyan", height: "100vh", width: "15rem" }}>
      Right
    </Box>
  );
};

export default Right;
